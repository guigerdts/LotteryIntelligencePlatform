"""Deep-learning model engines (Fase 8 — DLE-.. contracts).

Pure engine package mirroring ``app/ml/``.  Zero imports from ``ml/``;
quantize/checksum helpers are duplicated (~15 LOC) to enforce isolation (DLE-13).
"""
